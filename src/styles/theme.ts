const themeSettings = {
  font: {
    family: 'Lato, sans-serif',
    weights: {
      light: 300,
      regular: 400,
      bold: 700
    }
  },
  colors: {
    white: '#FFFFFF',
    black: '#000000'
  }
} as const;

export default themeSettings;
